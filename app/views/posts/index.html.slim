- title("Blog")
.row
  - @posts.each do |post|
    .col-xs-12.bottom-lined
      a href=post_path(post.slug)
        h3 =post.title
        span =post.created_at.strftime('%B %e, %Y')
      .redact
        p =raw(post.content.truncate(400))
      hr
